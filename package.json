{
  "name": "doctor-appointment-app",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "yarn install --workspaces",
    "dev:backend": "yarn dev -w backend",
    "dev:frontend": "yarn dev -w frontend",
    "build": "yarn build -w backend && yarn build -w frontend"
  }
}
